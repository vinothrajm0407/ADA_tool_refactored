flask>=3.0.0
gunicorn>=21.0.0
PyJWT>=2.8.0
bcrypt>=4.1.0

# Windows only: required for greenlet (Playwright dependency) — fixes "DLL load failed while importing _greenlet"
msvc-runtime; sys_platform == "win32"

# ADA check: pip install axe-playwright-python — Playwright + axe-core for execution and backend
axe-playwright-python>=0.1.6
playwright>=1.25.0

# MSSQL scan history (requires ODBC Driver 18 for SQL Server + MSSQL_CONN_STR)
pyodbc>=5.0.0
redis>=4.0.0
rq>=1.1.0
python-dotenv>=1.0.0
